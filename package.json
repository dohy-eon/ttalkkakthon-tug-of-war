{
  "name": "juldarigi",
  "version": "1.0.0",
  "description": "줄?다리기? - 모바일 기울기로 조작하는 실시간 웹 기반 줄?다리기? 게임",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "postinstall": "cd server && npm install && cd ../client/pc && npm install && cd ../mobile && npm install",
    "build": "cd client/pc && npm run build && cd ../mobile && npm run build",
    "start": "cd server && npm start",
    "dev": "cd server && npm run dev"
  }
}
